using UnityEngine;
using System.Collections;
using GlobalSet;

public class PlayCurrentMovie : MonoBehaviour {
    /*
	MovieTexture movieTexture;
	// Use this for initialization
	void Start () {
		string currentSelectMovieName = "BG_" + Global.currentSelectMode;
		movieTexture = RSC.Get (currentSelectMovieName) as MovieTexture;
		if (movieTexture == null) {
			movieTexture = Resources.Load ("Videos/" + currentSelectMovieName) as MovieTexture;
			if (movieTexture == null) {
				Debug.Log ("Failed to loading MovieTexture : " + currentSelectMovieName);
				return;
			}
		}
		GetComponent<Renderer>().material.mainTexture = movieTexture;
		movieTexture.loop = true;
		movieTexture.Play ();

	}
	
	// Update is called once per frame
	void Update () {
	
	}
    */
}
